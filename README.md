# mininet-zone-segmentation
A simulated enterprise network built with Mininet and POX, implementing SDN-based routing and firewall zone segmentation between department subnets, a protected server zone, and external trust boundaries.
